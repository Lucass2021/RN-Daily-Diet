import { Header } from "@components/Header"
import { Text, View } from "react-native"

export const Home = () => {
    return(
        <View>
            <Header
                showBackButton={false}
            />
        </View>
    )
}